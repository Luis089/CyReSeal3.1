<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
        <% if current_user %>
        <%= link_to fa_icon('shield fw', text: 'CyReSeal'),index_path, class:"navbar-brand"%>
        <ul class="navbar-nav">
            <li class="nav-item <%= 'active' if request.path == auditoren_path%>">
                <%= link_to fa_icon('suitcase fw', text: "Auditoren"), auditoren_path, class:"nav-link"%>
            </li>
        </ul>
        <%else%>
        <%= link_to fa_icon('shield fw', text: 'CyReSeal'),root_path, class:"navbar-brand"%>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item <%= 'active' if request.path == about_path%>">
                <%= link_to 'About', about_path, class:"nav-link"%>
            </li>
        </ul>
        <%end%>
        <% if current_user %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item user-name">
                <%= link_to fa_icon('user fw', text: "#{current_user.full_name}"),user_path(current_user), class:"nav-item user-name"%>
            </li>
            <li class="nav-item">
                <%= link_to 'Log out',  destroy_user_session_path, class:"btn btn-outline-danger loggout-button", method: :delete %>
            </li>
        </ul>
    </div>
    <% end%>
    </div>
</nav>