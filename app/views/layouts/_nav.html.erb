<nav class="navbar border-bottom navbar-expand-lg">
    <div class="container">
        <% if current_user %>
        <%= link_to fa_icon('shield-alt fw', text: 'CyReSeal'), welcome_path, class:"navbar-brand"%>
        <ul class="navbar-nav">
            <li class="nav-item <%= 'active' if current_page?(auditoren_path)%>">
                <%= link_to auditoren_path, class:"nav-link" do %>
                <%= fa_icon('suitcase fw') %>
                <span>Auditoren</span>
                <% end %>
            </li>
        </ul>
        <%else%>
        <%= link_to fa_icon('shield-alt fw', text: 'CyReSeal'),root_path, class:"navbar-brand"%>
        <ul class="navbar-nav ml-auto">
            <li><%= link_to fa_icon('sign-in-alt fw',text:"Anmelden"), new_user_session_path, class:"nav-link mr-5" %>
            </li>
            <li class="nav-item <%= 'active' if request.path == about_path%>">
                <%= link_to fa_icon('info-circle fw', text: "About"), about_path, class:"nav-link"%>
            </li>
        </ul>
        <%end%>
        <% if current_user %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item user-name">
                <%= link_to fa_icon('user fw', text: "#{current_user.full_name}"),user_path(current_user), class:"nav-item user-name"%>
            </li>
            <li class="nav-item">
                <%= link_to fa_icon('sign-out-alt fw', text: "Abmelden"),  destroy_user_session_path, method: :delete, class:"nav-link" %>
            </li>
        </ul>
    </div>
    <% end%>
    </div>
</nav>