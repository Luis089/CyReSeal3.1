<div id="page-content">
    <div class="container mt-4">
        <h2>Fragebogen auswählen, um Ergebnisse zu sehen: </h2>
        <% @quizzes.each do |result| %>
        <div class="row">
            <div class="col-6">
                <h3>
                    <div class="card card-header results-block mb-3">
                        <%= link_to  result.title, quiz_path(result) %>
                    </div>
                </h3>
            </div>
        </div>
        <% end %>
        <%= render 'layouts/messages' %>
        <% if current_user %>
        <%= link_to fa_icon('angle-left fw',text:"Zur Startseite"), root_path, class:"btn btn-info mt-2"%>
        <%= link_to fa_icon('angle-left fw',text:"Zur den Auditoren"), users_path, class:"btn btn-info mt-2"%>
        <% end %>
    </div>
</div>